# Module Specifications

Add one Markdown file per SystemVerilog module in this directory.

Each module document must use `../module-template.md` and must include complete
input and output signal tables before the module graduates from mock status.
